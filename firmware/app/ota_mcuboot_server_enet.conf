# Keep default IFR MCUboot flash-remap layout metadata; do not enable main-flash-only.
# Standalone linker override is applied from standalone_linker.cmake for P2 bring-up.
CONFIG_MCUX_PRJSEG_module.board.ota_examples.mcxn.default_linker_application=n
